/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add your image domains here when you add real image URLs
    // e.g. remotePatterns: [{ protocol: 'https', hostname: 'your-cdn.com' }]
    remotePatterns: [],
  },
}

module.exports = nextConfig
